SC withoutco
(* Sequential consistency, no co version *)

let invrf=(rf)-1

(* co observations in test *)
let obsco =
    (WW(po-loc)
    |(rf;RW(po-loc))
    |noid(WR(po-loc);invrf)
    |noid(rf;RR(po-loc);invrf))

let co = (obsco|co0) (* NB no transitive closure... *)
let fr = (invrf;co)
show co,fr

acyclic po | fr | rf | co
